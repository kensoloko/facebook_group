<% if signed_in? && current_user.is_group_creator?(group) %>
  <div id="join-form">
    <% if signed_in? && current_user.join?(group) %>
      <%= render "leave_group", leave_group: @leave_group %>
    <% else %>
      <%= render "join_group" %>
    <% end %>
  </div>
<% end %>
